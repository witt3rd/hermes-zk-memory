---
type: H
id: c742ef84
ts: 2026-08-20T16:50:52Z
from: hermes-zk-memory
subject: Retire HANDOFF.md; inhabit repo on the signalling protocol
---

# State

- Primary clone on `master` at `origin/master` tip; this worktree
  (feat/inhabit-signalling) retires HANDOFF.md in favor of the signalling event log. Clean
  apart from pre-existing untracked `uv.lock`.
- **43 passed** (`.venv/bin/python -m pytest`). Uses real hermes-agent at
  `/home/dt/.hermes/hermes-agent`; sibling `zk-memory` on `sys.path` via conftest.
- `AGENTS.md` is the single source of truth (`README.md` → symlink); `skills/caretaker/` added.

## What this is

A **thin `MemoryProvider` adapter** over `witt3rd/zk-memory` (host-agnostic zettelkasten
library). Same split shape as `hermes-prospecta`/`prospecta`.

## Current surface

- `__init__.py` — `ZkMemoryProvider` constructs `Memory`, delegates every op; keeps tool text,
  threading, root/config resolution, `register_auxiliary_task`. Sibling import is relative
  `from . import llm` with a flat-import fallback, so it loads under both hermes' package loader
  and pytest's flat import.
- `llm.py` — the `StructuredLLM` adapter: `TASK_KEY` (`zk_memory_judge`),
  `build_structured_llm(provider, model)`, `hermes_structured_llm(...)` (config-reading
  backward-compat wrapper), `_resolve_client`, `_forced_tool_call`. No baked provider/model —
  `initialize()` reads the whole `auxiliary.zk_memory_judge` block from the profile config;
  missing provider/model disables retain (llm=None) with a clear log, never a hermes default.
- Deps: `pyproject.toml` (`[lancedb]`) + `plugin.yaml` both pinned to `zk-memory @ ...@v0.4.0`.

## Version-pin history

The plugin follows zk-memory tags: `@v0.1.0` → `@v0.2.0` → `@v0.3.0` → `@v0.4.0`. **Keep both
`pyproject.toml` and `plugin.yaml` in sync** — a past release left `plugin.yaml` stale.

## Gotchas

- `tests/conftest.py` sibling path is hardcoded (`/home/dt/src/witt3rd/zk-memory`), not derived
  from `REPO_ROOT.parent` (worktree lives under `hermes-zk-memory.wt/`).
- Operator install needs `zk-memory` importable; symlink-only installs `ImportError`.
- Import shape is load-bearing — bare `import llm` fails under hermes' loader (silently); keep
  the relative + fallback shape.
- Backticks in `git commit -m` get shell-substituted — use `-F file`.

## Next

1. Merge this inhabitation PR; the repo is then on the signalling protocol (no HANDOFF.md —
   use `scripts/agent handoff`).
2. Optional: register `skills/` in fleet-ops (`~/.agents/skills/hermes-zk-memory` → repo
   `skills/`).
3. If the fleet shared-corpus deployment lands, wire `Memory.tend_writes` to a caretaker
   host/agent and document the `zk-memory` pip dep in being-plugin / fleet installers.
